/*
import javax.sound.midi.SoundbankResource;
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

*/
/**
 * Created by mangospring on 12/12/16.
 *//*

public class TestHack {
    public static void main(String[] args) {
       checkPrime(2);
        checkPrime(2,1);
        checkPrime(2,1,3);
        checkPrime(2,1,3,4);
        checkPrime(2,1,3,4,5);
        getFood("cake");
    }

     String getFood(String type){
        String t;
        try {
            if(type.equalsIgnoreCase(this.getClass().getName())){
                t=this.get.getName();
            }
        } catch (ClassNotFoundException e) {
            e.printStackTrace();
        }
        return null;
    }

    public static void checkPrime(int... vars){

        for(int i=0;i<vars.length;i++){
            boolean flag=true;
            for(int j=2;j<vars[i];j++){
                if(vars[i]<=1||(vars[i]%j==0&&vars[i]!=2)) {
                    flag = false;
                }
            }
            if(flag&&vars[i]>=2)
                System.out.print(vars[i]+" ");
        }
        System.out.println("");
    }
    static class Inner{
        private class Private{
            private void power(int num){
                System.out.println(num);
            }
        }
    }
}
*/
